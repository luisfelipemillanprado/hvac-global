---
description: Type aliases declared above interfaces, and first in the file
globs: **/*.{ts,tsx}
alwaysApply: false
---

# Type / interface ordering · Orden de types e interfaces

|        |                                                                                                                                                                                                     |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **EN** | In any file that declares both, put **`type` aliases first and on top**, then the `interface` declarations below. Keep dependency order within each group (a `type` used by another goes above it). |
| **ES** | En cualquier archivo que declare ambos, los **`type` van primero y arriba**, y debajo las `interface`. Respetar el orden de dependencia dentro de cada grupo (un `type` usado por otro va encima).  |

```ts
// ✅ GOOD — types first, then interfaces
export type DashboardBadgeIcon = 'bolt' | 'arrowRight'
export type DashboardBadgeIconPosition = 'left' | 'right'

export interface DashboardBadgeProps {
  label: string
  icon?: DashboardBadgeIcon
  iconPosition?: DashboardBadgeIconPosition
}
```

```ts
// ❌ BAD — interface above the types it uses
export interface DashboardBadgeProps {
  label: string
  icon?: DashboardBadgeIcon
}

export type DashboardBadgeIcon = 'bolt' | 'arrowRight'
```
